{% extends 'base.html.twig' %}

{% block title %}modification du profil de {{users.name}} {{users.surname}}{% endblock %}

{% block body %}

<div class="container" style="padding-top: 50px;">
    <h5 class="card-title text-center">Inscription</h5>
    {{form_start(form)}}
        {{ form_row(form.name, {'label': 'Nom:* '}) }}
        {{ form_row(form.surname, {'label': 'Prenom:* '}) }}
        {{ form_row(form.email, {'label': 'Adresse Mail:* '}) }}
        {{ form_row(form.imageFile, {'label': 'Image de profil:* '}) }}
        {{ form_row(form.password, {'label': 'Mot de passe:* '}) }}
        {{ form_row(form.verifPassword, {'label': 'Saisir à nouveau votre mot de passe:* '}) }}
        <input type="submit" class="btn btn-primary" value="Valider">
    {{form_end(form)}}
</div>

{% endblock %}
